# -----------------------------------------------------------------------------
# Optional settings for ./build.sh — copy to build.env and edit.
# File build.env is gitignored and is not pushed to GitHub.
#
# Location: repository root, next to build.sh.
#
# Precedence: variables you export in the shell before running ./build.sh override
# values from build.env (same pattern as otheruser/nzbgetvpn build.env).
# -----------------------------------------------------------------------------

# Docker Hub repository (namespace/name, NO tag). Used for image tags and docker pushrm.
DOCKER_IMAGE_REPO=otheruser/restic-backup-helper

# Passed to: docker buildx build --platform ...
BUILD_PLATFORM=linux/amd64,linux/arm64

# Must match the Restic base image tag patched into Dockerfile (FROM restic/restic:...).
# CLI alternative: ./build.sh --base 0.18.2
# Use 'newest' (alias 'latest') to auto-resolve via 'docker run --rm restic/restic:latest version'.
# Use 'prerelease' (aliases 'rc' / 'beta') to auto-resolve the newest Docker Hub rc/beta image tag.
# Any other non-version value is rejected before the build starts.
# VERSION_RESTIC=0.18.1
